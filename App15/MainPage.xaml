<Page
    x:Class="App15.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App15"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Rectangle Grid.Row="0" Fill="#FFB09F71"></Rectangle>
        <Rectangle Grid.Column="1" Fill="#FFB09F71"></Rectangle>
        <Rectangle Grid.Column="2" Fill="#FFB09F71"></Rectangle>
        <Rectangle Grid.Row="1" Fill="#FF91CBCB"></Rectangle>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>

            <ColumnDefinition Width="5*" ></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
        </Grid>
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="20,0,0,0">
            <TextBlock FontSize="36">Welcome to the application</TextBlock>
            <TextBlock>Below enter the task you want to add</TextBlock>
            <TextBox Name="UserInputTaskName"></TextBox>
            <Button>Click this</Button>
            <TextBlock FontSize="50" Margin="0,20,0,0" Text="{Binding ElementName=UserInputTaskName, Path=Text}"></TextBlock>
            
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="3" Margin="20,0,0,0" >
             <!--will be nice to add pictures into the items on the to do list as well-->
            <CheckBox>This is a sample item on the to do list</CheckBox>
            <CheckBox>This is another item on the to do list</CheckBox>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0">
            <TextBlock FontSize="20">Student Manager</TextBlock>
            <Button HorizontalAlignment="Stretch" Margin="10,10,0,0">Home Page</Button>
        </StackPanel>
    </Grid>
</Page>
